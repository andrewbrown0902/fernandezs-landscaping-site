---
---
<div class="mobile-call-bar">
  <a href="tel:5743862740" class="mcb-btn primary">
    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/></svg>
    Call Noah
  </a>
  <a href="sms:5743862740" class="mcb-btn secondary">
    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/></svg>
    Text
  </a>
  <a href="#quote" class="mcb-btn accent" data-quote-open>
    Free Quote
  </a>
</div>

<style>
  .mobile-call-bar {
    display: none;
    position: fixed;
    bottom: 0;
    left: 0;
    right: 0;
    background: var(--asphalt);
    border-top: 2px solid var(--pop);
    padding: 10px 10px calc(10px + env(safe-area-inset-bottom));
    gap: 8px;
    z-index: 90;
    box-shadow: 0 -4px 20px rgba(0,0,0,0.2);
  }
  .mcb-btn {
    flex: 1;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 6px;
    padding: 13px 8px;
    font-family: var(--font-display);
    font-weight: 600;
    font-size: 0.92rem;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    border-radius: var(--radius);
    text-decoration: none;
    border: 1.5px solid transparent;
    transition: background 0.15s ease, color 0.15s ease;
  }
  .mcb-btn svg { flex-shrink: 0; }
  /* Primary action: big green */
  .mcb-btn.primary {
    background: var(--pop);
    color: var(--charcoal);
    border-color: var(--pop);
  }
  /* Secondary: dark outlined */
  .mcb-btn.secondary {
    background: transparent;
    color: var(--bone);
    border-color: rgba(242, 238, 227, 0.4);
    flex: 0.75;
  }
  /* Tertiary: forest bg */
  .mcb-btn.accent {
    background: var(--forest);
    color: var(--bone);
    border-color: var(--forest);
  }

  @media (max-width: 768px) {
    .mobile-call-bar { display: flex; }
    body { padding-bottom: 68px; }
  }
</style>
